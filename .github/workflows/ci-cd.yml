# ============================================
# CI/CD Pipeline — Lint → Security → Test → Build → Push
# ============================================
# Required GitHub Secrets:
#   DOCKERHUB_USERNAME  - Docker Hub username
#   DOCKERHUB_TOKEN     - Docker Hub access token (not your password)
#
# Flow:
#   • Every push / PR → lint + security + test
#   • Push to master only → build & push Docker image
# ============================================

name: CI/CD Pipeline

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

# # ─── Limit permissions to principle of least privilege ───────────────────────
# permissions:
#   contents: read
#   packages: write # needed to push to GHCR if ever switched from DockerHub
#   security-events: write #

# # ─── Cancel redundant runs on the same branch/PR ────────────────────────────
# concurrency:
#   group: ${{ github.workflow }}-${{ github.ref }}
#   cancel-in-progress: true

env:
  NODE_VERSION: "20"
  DOCKER_IMAGE: ${{ secrets.DOCKERHUB_USERNAME }}/inventory-app

# ─── Shared test environment variables ──────────────────────────────────────
# Defined once here, referenced in each test step via ${{ env.* }}
# to avoid duplication and drift between steps.
defaults:
  run:
    shell: bash

jobs:
  # ─── Lint ──────────────────────────────────────────────────────────────────
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint

  # ─── Security Scans ────────────────────────────────────────────────────────
  # security:
  #   name: DevSecOps Scans
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #     security-events: write
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0 # full history required for Gitleaks

  #     # Secret scanning — MUST block on secrets in code, never continue-on-error
  #     - name: Secret Scanning (Gitleaks)
  #       uses: gitleaks/gitleaks-action@v2
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #       # continue-on-error removed intentionally: secrets in code = hard fail

  #       # Dependency & filesystem vulnerability scan
  #       # exit-code: '1' ensures HIGH/CRITICAL vulns fail the build
  #     - name: Dependency Scanning (Trivy SCA)
  #       uses: aquasecurity/trivy-action@0.28.0
  #       with:
  #         scan-type: "fs"
  #         scan-ref: "."
  #         format: "sarif"
  #         output: "trivy-results.sarif"
  #         exit-code: "1" # fail on HIGH/CRITICAL — was '0' (silent pass)
  #         severity: "HIGH,CRITICAL"
  #         scanners: "vuln,secret,config"

  #     - name: Upload Trivy SARIF to GitHub Security tab
  #       uses: github/codeql-action/upload-sarif@v3
  #       if: always() # upload even on failure so results are visible
  #       with:
  #         sarif_file: "trivy-results.sarif"

  #     # SAST — pinned to a specific release tag, not @master (supply-chain safety)
  #     - name: SAST (njsscan)
  #       uses: ajinabraham/njsscan-action@v7
  #       with:
  #         args: ". --exit-warning"
  #       continue-on-error: true # SAST findings are advisory, not blocking yet

  # ─── Test ──────────────────────────────────────────────────────────────────
  # test:
  #   name: Test
  #   runs-on: ubuntu-latest
  #   needs: [ lint, security ]

  #   # Service containers — started before steps
  #   services:
  #     postgres:
  #       image: postgres:16-alpine
  #       env:
  #         POSTGRES_USER: admin
  #         POSTGRES_PASSWORD: admin
  #         POSTGRES_DB: inventory_app_test
  #       ports:
  #         - 5432:5432
  #       options: >-
  #         --health-cmd "pg_isready -U admin -d inventory_app_test"
  #         --health-interval 10s --health-timeout 5s --health-retries 5

  #     redis:
  #       image: redis:7-alpine
  #       # Start Redis with a password to match the app's BullMQ configuration
  #       options: >-
  #         --health-cmd "redis-cli -a testpass ping" --health-interval 10s
  #         --health-timeout 5s --health-retries 5
  #       ports:
  #         - 6379:6379

  #   # Test env vars defined once at job level — no duplication across steps
  #   env:
  #     NODE_ENV: test
  #     DB_HOST: localhost
  #     DB_PORT: 5432
  #     DB_USERNAME: admin
  #     DB_PASSWORD: admin
  #     DB_DATABASE: inventory_app_test # fixed: was DB_NAME, app reads DB_DATABASE
  #     DB_TYPE: postgres
  #     REDIS_HOST: localhost
  #     REDIS_PORT: 6379
  #     REDIS_PASSWORD: testpass # added: app now requires Redis auth
  #     JWT_SECRET: test_jwt_secret_ci
  #     JWT_EXPIRES_IN: 3600s

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: "npm"

  #     - name: Install dependencies
  #       run: npm ci

  #     # Run tests with coverage in one pass — was run twice before (wasted CI time)
  #     - name: Run tests with coverage
  #       run: npm run test:cov -- --passWithNoTests --forceExit

  #     - name: Upload coverage report
  #       uses: actions/upload-artifact@v4
  #       if: always()
  #       with:
  #         name: coverage-report
  #         path: coverage/
  #         retention-days: 7

  # ─── Build & Push Docker Image ─────────────────────────────────────────────
  docker:
    name: Build & Push Docker
    runs-on: ubuntu-latest
    # needs: test
    # Only deploy on pushes to master — not on PRs
    if: github.ref == 'refs/heads/master' && github.event_name == 'push'
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # QEMU enables multi-arch builds (amd64 + arm64 for mixed node clusters)
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKER_IMAGE }}
          tags: |
            # Git SHA tag for traceability (e.g. sha-a1b2c3d)
            type=sha,prefix=sha-,format=short
            # Semantic version tag when you push a git tag like v1.2.3
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            # latest always points to the most recent master build
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # Build for both amd64 (standard servers) and arm64 (Apple M1, ARM nodes)
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
