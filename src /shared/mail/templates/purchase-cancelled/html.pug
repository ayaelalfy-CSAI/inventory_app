doctype html
html
  head
    meta(charset="UTF-8")
    title Purchase Cancelled
    style.
      body {
        font-family: Arial, sans-serif;
        background-color: #f9f9f9;
        padding: 20px;
      }
      .container {
        background-color: #fff;
        border-radius: 8px;
        padding: 20px;
        box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      }
      h2 { color: #b91c1c; }
      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 15px;
      }
      th, td {
        border: 1px solid #ddd;
        padding: 8px;
        text-align: left;
      }
      th {
        background-color: #f0f0f0;
      }
      .footer {
        margin-top: 30px;
        font-size: 13px;
        color: #777;
      }
  body
    .container
      h2 Purchase Order Cancelled ❌
      p Hello #{user.name},
      p The purchase order ##{purchaseId} for branch <strong>#{branchName}</strong> has been cancelled.
      p The stock quantities have been adjusted accordingly.

      if items.length
        h3 Cancelled Items
        table
          thead
            tr
              th Variant ID
              th Quantity
          tbody
            each item in items
              tr
                td #{item.variantId}
                td #{item.quantity}

      .footer
        p This is an automated message from <strong>My App</strong>.
        p Please do not reply directly to this email.
