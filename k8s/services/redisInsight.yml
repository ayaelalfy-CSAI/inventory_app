apiVersion: v1
kind: Service
metadata:
  name: redis-insight-service
  labels:
    app: redis-insight
  namespace: inventory  
spec:
  type: ClusterIP
  selector:
    app: redis-insight
  ports:
  - port: 5540
    targetPort: 5540
