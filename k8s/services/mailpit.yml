apiVersion: v1
kind: Service
metadata:
  name: mailpit-service
  labels:
    app: mailpit
  namespace: inventory  
spec:
  type: ClusterIP
  selector:
    app: mailpit
  ports:
  - port: 8025
    targetPort: 8025
    name: web-ui
  - port: 1025
    targetPort: 1025
    name: smtp  
