apiVersion: v1
kind: Service
metadata:
  name: postgres-svc
  labels:
    app: postgres
  namespace: inventory  
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432