apiVersion: v1
kind: Service
metadata:
  name: pgadmin-svc
  namespace: inventory
  labels:
    app: pgadmin
spec:
  type: ClusterIP
  selector:
    app: pgadmin
  ports:
    - name: web-ui
      port: 80
      targetPort: 80