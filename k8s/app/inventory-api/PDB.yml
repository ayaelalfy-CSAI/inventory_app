apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: inventory-app-pdb
  namespace: inventory-prod
spec:
  minAvailable: 1              # always keep at least 1 pod alive during node drain
  selector:
    matchLabels:
      app: inventory-app